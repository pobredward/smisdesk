# 🔐 관리자 페이지 - FAQ 관리 시스템

## ✅ 구현 완료!

FAQ를 쉽게 추가/수정/삭제할 수 있는 관리자 페이지가 완성되었습니다.

## 🚀 접속 방법

### URL
```
http://localhost:3000/admin
```

### 로그인 정보
- **비밀번호**: `smis2024`

## 📋 주요 기능

### 1. 간단한 비밀번호 인증
- 로그인 페이지로 보호
- 로컬 스토리지에 세션 저장
- 로그아웃 기능

### 2. FAQ 목록 조회
- 전체 FAQ 목록 표시
- 카테고리별 태그
- 키워드 표시
- 최신순 정렬

### 3. FAQ 추가
- 질문, 답변 입력
- 카테고리 설정
- 키워드 설정 (쉼표로 구분)
- 실시간 Firestore 저장

### 4. FAQ 수정
- 기존 FAQ 선택하여 수정
- 모든 필드 편집 가능
- 업데이트 시간 자동 기록

### 5. FAQ 삭제
- 삭제 확인 다이얼로그
- Firestore에서 즉시 삭제

## 🛡️ Firestore Security Rules 업데이트 필요

관리자 페이지에서 FAQ를 추가/수정/삭제하려면 Firestore Rules를 업데이트해야 합니다.

### 방법 1: 테스트 모드 (간단, 개발용)

Firebase 콘솔 > Firestore Database > Rules:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /faqs/{faqId} {
      allow read: if true;
      allow write: if true;  // 테스트 모드 (모두 허용)
    }
  }
}
```

⚠️ **주의**: 이 방식은 누구나 데이터를 수정할 수 있으므로 **개발 환경에서만** 사용하세요!

### 방법 2: Firebase Auth 연동 (프로덕션 권장)

나중에 Firebase Authentication을 추가하여 인증된 관리자만 쓰기 가능하도록 설정:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /faqs/{faqId} {
      allow read: if true;
      allow write: if request.auth != null && 
                      request.auth.token.email == 'admin@smis.co.kr';
    }
  }
}
```

## 💡 사용 방법

### 1. FAQ 추가하기

1. http://localhost:3000/admin 접속
2. 비밀번호 `smis2024` 입력
3. "새 FAQ 추가" 버튼 클릭
4. 폼 작성:
   - **질문**: 사용자가 물어볼 질문
   - **답변**: 상세한 답변 (여러 줄 가능)
   - **카테고리**: 예) 캠프 정보, 생활 관리, 프로그램
   - **키워드**: 예) 인원, 반, 그룹 (쉼표로 구분)
5. "추가" 버튼 클릭

### 2. FAQ 수정하기

1. FAQ 목록에서 수정할 항목의 연필(✏️) 아이콘 클릭
2. 내용 수정
3. "수정" 버튼 클릭

### 3. FAQ 삭제하기

1. FAQ 목록에서 삭제할 항목의 휴지통(🗑️) 아이콘 클릭
2. 확인 다이얼로그에서 "확인" 클릭

## 🎨 화면 구성

### 로그인 화면
```
┌─────────────────────────────┐
│     🔒 관리자 로그인         │
│                             │
│  비밀번호 입력 [_________]  │
│                             │
│      [    로그인    ]       │
└─────────────────────────────┘
```

### 관리자 대시보드
```
┌────────────────────────────────────────┐
│  FAQ 관리자          [홈] [로그아웃]    │
├────────────────────────────────────────┤
│                                        │
│  ┌──────────────────────────────────┐ │
│  │ FAQ 추가/수정 폼                  │ │
│  │                                  │ │
│  │  질문: [________________]        │ │
│  │  답변: [________________]        │ │
│  │  카테고리: [__________]          │ │
│  │  키워드: [_____________]         │ │
│  │                                  │ │
│  │       [취소] [저장]              │ │
│  └──────────────────────────────────┘ │
│                                        │
│  ┌──────────────────────────────────┐ │
│  │ FAQ 목록 (11개)                  │ │
│  ├──────────────────────────────────┤ │
│  │ [캠프정보] 2024-04-14            │ │
│  │ 캠프 인원이 몇 명인가요?    ✏️ 🗑️ │ │
│  │ 120명의 학생과...                │ │
│  │ #인원 #반 #그룹                  │ │
│  ├──────────────────────────────────┤ │
│  │ [생활관리] 2024-04-14            │ │
│  │ 용돈은 어떻게 관리하나요?  ✏️ 🗑️  │ │
│  │ 용돈봉투에 넣어서...             │ │
│  │ #용돈 #관리                      │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

## 🔄 실시간 동기화

- FAQ 추가/수정/삭제 시 즉시 Firestore에 반영
- 홈페이지 챗봇은 실시간으로 최신 FAQ 사용
- 별도의 배포나 재시작 불필요

## 📱 반응형 디자인

- 데스크탑 및 모바일에서 모두 사용 가능
- 터치 친화적 UI

## ⚠️ 중요 참고사항

### 1. Firestore Rules 설정 필수
관리자 페이지를 사용하려면 위의 Security Rules를 **반드시** 업데이트하세요.

### 2. 비밀번호 변경
실제 운영 시 `app/admin/page.tsx` 파일의 `ADMIN_PASSWORD` 변수를 변경하세요:

```typescript
const ADMIN_PASSWORD = 'your_secure_password';
```

### 3. 프로덕션 배포 시
- Firebase Authentication 추가 권장
- 환경 변수로 비밀번호 관리
- HTTPS 필수

## 🎯 다음 단계

### 지금 바로 사용 가능:
1. Firestore Rules 업데이트
2. http://localhost:3000/admin 접속
3. FAQ 관리 시작!

### 추후 개선 사항 (선택):
- Firebase Authentication 연동
- 관리자 역할 권한 관리
- FAQ 일괄 가져오기/내보내기
- 통계 대시보드
- 검색 기능

---

**모든 준비가 완료되었습니다!** 

이제 Firebase 콘솔에서 Security Rules만 업데이트하면 바로 사용할 수 있습니다! 🚀
