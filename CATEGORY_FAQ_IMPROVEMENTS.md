# 🎨 카테고리 및 FAQ 관리 개선 가이드

## ✨ 새로운 기능

### 1️⃣ 카테고리 드래그 앤 드롭 순서 변경
카테고리를 원하는 순서로 드래그하여 정렬할 수 있습니다.

**사용 방법:**
1. 카테고리 관리 페이지 열기
2. 카테고리 왼쪽의 ≡ 아이콘을 드래그
3. 원하는 위치에 드롭
4. 순서가 자동으로 Firestore에 저장됩니다

**특징:**
- 실시간 순서 업데이트
- FAQ 목록과 필터에도 즉시 반영
- 편집 모드가 아닐 때만 드래그 가능

---

### 2️⃣ 시각적 색상 선택기
12가지 색상 팔레트에서 시각적으로 색상을 선택할 수 있습니다.

**사용 방법:**
1. 카테고리 편집 모드 진입 (연필 아이콘 클릭)
2. "색상 선택" 섹션에서 원하는 색상 클릭
3. 미리보기에서 결과 확인
4. "저장" 클릭

**특징:**
- 12가지 프리셋 색상 제공
- 실시간 미리보기
- 선택한 색상에 체크마크 표시
- 호버 효과로 사용성 향상

**색상 팔레트:**
```
파란색, 보라색, 초록색, 주황색, 빨간색, 남색
분홍색, 노란색, 청록색, 하늘색, 연두색, 호박색
```

---

### 3️⃣ FAQ 목록에서 빠른 카테고리 변경
FAQ를 편집하지 않고도 목록에서 바로 카테고리를 변경할 수 있습니다.

**사용 방법:**
1. FAQ 목록에서 카테고리 드롭다운 클릭
2. 새 카테고리 선택
3. 확인 메시지에서 "확인"
4. 즉시 변경 적용

**특징:**
- 확인 메시지로 실수 방지
- 즉시 Firestore 업데이트
- 변경 후 자동 새로고침
- 드롭다운에 현재 카테고리 색상 표시

---

## 🎯 실전 사용 예시

### 시나리오 1: 카테고리 순서 정리
**목표:** 자주 사용하는 카테고리를 앞으로 이동

**절차:**
1. "카테고리 관리" 클릭
2. "식단" 카테고리를 드래그
3. "등록" 카테고리 앞으로 드롭
4. 자동 저장 완료!

**결과:**
```
Before: #1 등록, #2 프로그램(수업), ... #9 식단
After:  #1 식단, #2 등록, #3 프로그램(수업), ...
```

---

### 시나리오 2: 카테고리 색상 변경
**목표:** "환자" 카테고리를 빨간색으로 강조

**절차:**
1. "환자" 카테고리 옆 연필 아이콘 클릭
2. 색상 팔레트에서 빨간색 선택
3. 미리보기 확인: "미리보기: 환자" (빨간색)
4. "저장" 클릭

**결과:**
- FAQ 목록의 "환자" 태그가 빨간색으로 표시
- 카테고리 필터 버튼도 빨간색으로 변경

---

### 시나리오 3: FAQ 카테고리 일괄 변경
**목표:** 잘못된 카테고리의 FAQ 10개를 빠르게 수정

**절차:**
1. 카테고리 필터에서 "생활&숙소" 선택
2. 첫 번째 FAQ의 카테고리 드롭다운 클릭
3. "식단" 선택
4. 확인
5. 나머지 9개도 동일하게 반복

**장점:**
- 편집 모드 진입 불필요
- 한 번의 클릭으로 변경
- 빠른 대량 작업 가능

---

## 📊 Before & After 비교

### Before (이전)
```
❌ 카테고리 순서 변경: 코드 수정 필요
❌ 색상 선택: select 박스에서 "색상 1", "색상 2" 선택
❌ FAQ 카테고리 변경: 편집 모드 진입 → 드롭다운 선택 → 저장
```

### After (개선)
```
✅ 카테고리 순서 변경: 드래그 앤 드롭으로 즉시 변경
✅ 색상 선택: 시각적 팔레트에서 클릭으로 선택
✅ FAQ 카테고리 변경: 목록에서 드롭다운 선택만으로 변경
```

---

## 🎨 UI/UX 개선사항

### 1. 카테고리 관리 페이지
- **드래그 핸들**: ≡ 아이콘으로 명확한 드래그 가능 표시
- **호버 효과**: 마우스 오버 시 배경색 변경
- **드래그 중 시각 효과**: 드래그 중인 항목 반투명 처리
- **편집 모드 강조**: 파란색 배경으로 편집 중 표시

### 2. 색상 선택기
- **6x2 그리드 레이아웃**: 한눈에 모든 색상 확인
- **선택 표시**: 체크마크로 현재 선택 표시
- **미리보기**: 실시간으로 변경 결과 확인
- **접근성**: 각 색상에 제목(title) 속성 추가

### 3. FAQ 카테고리 드롭다운
- **현재 색상 유지**: 드롭다운이 현재 카테고리 색상 표시
- **확인 메시지**: 실수 방지를 위한 확인 대화상자
- **즉시 반영**: 변경 후 자동 새로고침

---

## 💡 사용 팁

### 카테고리 순서 관리
1. **자주 사용하는 카테고리는 앞으로**
   - 등록, 프로그램(수업), 야외활동 등
2. **비슷한 카테고리는 그룹화**
   - 원어민 선생님, 멘토 선생님
   - 생활&숙소, 식단
3. **관리용 카테고리는 뒤로**
   - 기타, 임시 등

### 색상 활용
1. **중요도로 구분**
   - 빨간색: 환자, 긴급
   - 파란색: 일반 정보
   - 초록색: 활동
2. **유사 항목은 비슷한 색상**
   - 선생님 관련: 남색, 보라색
   - 생활 관련: 노란색, 주황색
3. **대비 고려**
   - 인접한 카테고리는 다른 계열 색상

### 빠른 FAQ 정리
1. **필터 활용**
   ```
   1. "생활&숙소" 필터 선택
   2. 잘못된 FAQ만 표시
   3. 드롭다운으로 일괄 변경
   ```
2. **검색 후 변경**
   ```
   1. 특정 키워드로 검색 (향후 기능)
   2. 관련 FAQ 표시
   3. 카테고리 일괄 변경
   ```

---

## 🔧 기술 세부사항

### 드래그 앤 드롭 구현
```typescript
// 드래그 시작
const handleDragStart = (index: number) => {
  setDraggedIndex(index);
};

// 드롭
const handleDrop = async (dropIndex: number) => {
  const newCategories = [...categories];
  const [draggedItem] = newCategories.splice(draggedIndex, 1);
  newCategories.splice(dropIndex, 0, draggedItem);
  
  // Firestore batch update
  await reorderCategories(newCategories);
};
```

### 색상 선택기
```typescript
// 12가지 색상 팔레트
const COLOR_PALETTE = [
  'bg-blue-100 text-blue-700',
  'bg-purple-100 text-purple-700',
  // ... 나머지 10개
];

// 색상 선택
<button onClick={() => setColor(color)} className={color}>
  {selectedColor === color && <Check />}
</button>
```

### FAQ 카테고리 변경
```typescript
// 드롭다운 onChange
onChange={async (e) => {
  const newCategory = e.target.value;
  if (confirm('변경하시겠습니까?')) {
    await updateDoc(faqRef, { 
      category: newCategory,
      updatedAt: Timestamp.now()
    });
    loadFAQs();
  }
}}
```

---

## 📱 반응형 디자인

### 데스크톱 (1024px 이상)
- 색상 팔레트: 6열
- FAQ 목록: 전체 너비
- 카테고리 버튼: 5열

### 태블릿 (768px ~ 1023px)
- 색상 팔레트: 6열 유지
- FAQ 목록: 패딩 조정
- 카테고리 버튼: 3-5열

### 모바일 (767px 이하)
- 색상 팔레트: 6열 (작은 버튼)
- FAQ 목록: 세로 스크롤
- 카테고리 버튼: 3열

---

## 🎓 고급 활용

### 1. 카테고리 순서로 중요도 표현
```
#1 긴급공지 (빨간색)
#2 등록 (파란색)
#3 프로그램(수업) (보라색)
...
#9 기타 (회색)
```

### 2. 색상 코딩 시스템
```
🔴 빨간색: 긴급, 중요, 주의
🟣 보라색: 교육, 학습
🟢 초록색: 활동, 체험
🟡 노란색: 생활, 일상
🔵 파란색: 일반 정보
```

### 3. FAQ 재분류 프로세스
```
1. 부적절한 카테고리 찾기
   → 필터로 카테고리별 확인

2. 새 카테고리 필요 시
   → "카테고리 관리"에서 추가

3. FAQ 일괄 재배치
   → 목록에서 드롭다운으로 변경

4. 카테고리 순서 조정
   → 드래그 앤 드롭으로 정렬

5. 불필요한 카테고리 삭제
   → FAQ 없는 카테고리 정리
```

---

## 🚀 빠른 시작

### 즉시 사용 가능!

```bash
# 1. 개발 서버 실행 (이미 실행 중이라면 생략)
npm run dev

# 2. 관리자 페이지 접속
http://localhost:3000/admin

# 3. 로그인
비밀번호: smis2024

# 4. "카테고리 관리" 클릭

# 5. 기능 사용
✅ 카테고리 드래그로 순서 변경
✅ 연필 아이콘 → 색상 팔레트에서 선택
✅ FAQ 목록에서 카테고리 드롭다운 변경
```

---

**업데이트 날짜:** 2026-04-13  
**버전:** 1.1.0  
**작성자:** SMIS Team
