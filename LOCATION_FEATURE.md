# 🌍 캠프 위치 기능 추가 완료!

## ✅ 구현 완료 사항

### 1. 타입 정의 (`lib/types.ts`)
- ✅ `LocationType` 정의: `'je' | 's' | 'f' | 'common'`
- ✅ `LOCATIONS` 상수 추가 (이모지, 이름, 설명 포함)
- ✅ `FAQ` 인터페이스에 `location` 필드 추가
- ✅ `ChatMessage` 인터페이스에 `location` 필드 추가

### 2. 관리자 페이지 (`app/admin/page.tsx`)
#### 📝 FAQ 입력 폼
- ✅ 캠프 위치 선택 드롭다운 추가
- ✅ 기본값: 제주캠프 (`'je'`)
- ✅ 공통 FAQ 선택 가능 (`'common'`)
- ✅ 각 캠프 이모지와 이름 표시

#### 🔍 FAQ 필터링
- ✅ 캠프별 필터 추가 (헤더 우측)
- ✅ 카테고리 + 캠프 이중 필터링
- ✅ "공통" FAQ는 모든 캠프에서 표시

#### 📋 FAQ 목록
- ✅ 각 FAQ에 위치 배지 표시
- ✅ 색상 구분:
  - 제주: 파란색
  - 싱가포르&말레이: 초록색
  - 말레이 가족: 보라색
  - 공통: 회색

### 3. 챗봇 위젯 (`components/ChatWidget.tsx`)
#### 🏷️ 헤더
- ✅ 캠프 선택 드롭다운 추가 (우측)
- ✅ 선택한 캠프에 맞는 FAQ만 표시
- ✅ 깔끔한 UI (흰색 배경 + 투명도)

#### 💬 대화
- ✅ 선택한 캠프의 FAQ + 공통 FAQ만 검색
- ✅ 메시지에 캠프 위치 표시
- ✅ placeholder 텍스트 동적 변경

#### 💡 추천 질문
- ✅ 선택한 캠프의 FAQ만 추천
- ✅ 카테고리 + 캠프 이중 필터링

### 4. 마이그레이션 스크립트
- ✅ `scripts/add-location-to-faqs.ts` 생성
- ✅ 기존 FAQ에 `location: 'je'` 자동 추가
- ✅ 이미 location 필드가 있으면 스킵

---

## 📊 캠프 위치 코드

| 코드 | 이모지 | 캠프 이름 |
|------|--------|-----------|
| `je` | 🏝️ | SMIS 제주캠프 |
| `s` | 🌴 | SMIS 싱가포르&말레이시아 주니어캠프 |
| `f` | 👨‍👩‍👧‍👦 | SMIS 말레이시아 가족캠프 |
| `common` | 🌏 | 공통 (모든 캠프) |

---

## 🚀 사용 방법

### 1. 기존 FAQ 마이그레이션 (필수)

```bash
npm run migrate:add-location
```

**결과:**
```
📌 FAQ에 location 필드 추가 시작...

✅ 업데이트: "캠프 인원은?" → location: "je"
✅ 업데이트: "용돈 관리는?" → location: "je"
...

============================
🎉 마이그레이션 완료!
  - 업데이트된 FAQ: 10개
  - 스킵된 FAQ: 0개
  - 총 FAQ: 10개
============================
```

### 2. 관리자 페이지 사용

#### FAQ 추가
1. http://localhost:3000/admin 접속
2. "FAQ 추가" 클릭
3. 질문, 답변, 카테고리, **캠프 위치** 선택
4. "추가" 클릭

#### 캠프별 FAQ 확인
1. 우측 상단 "캠프 선택" 드롭다운
2. 원하는 캠프 선택
3. 해당 캠프 + 공통 FAQ만 표시

### 3. 챗봇 사용

#### 유저 입장
1. http://localhost:3000 접속
2. 우측 상단에서 캠프 선택 (기본: 제주캠프)
3. 카테고리 선택 (선택사항)
4. 질문 입력 또는 추천 질문 클릭

---

## 💡 사용 예시

### 예시 1: 제주캠프 FAQ 추가

```
질문: 이번 캠프 참여 인원은?
답변: 28기 제주 여름캠프 기준 학생 약 120명...
카테고리: 등록
캠프 위치: 🏝️ SMIS 제주캠프
```

### 예시 2: 공통 FAQ 추가

```
질문: 휴대폰은 어떻게 관리하나요?
답변: 입소 당일 담당 선생님이 수거...
카테고리: 생활&숙소
캠프 위치: 🌏 공통 (모든 캠프)
```
→ 모든 캠프에서 이 FAQ가 표시됩니다!

### 예시 3: 싱가포르 캠프 FAQ 추가

```
질문: 싱가포르에서는 어디를 방문하나요?
답변: 마리나 베이 샌즈, 센토사 섬...
카테고리: 야외활동
캠프 위치: 🌴 SMIS 싱가포르&말레이시아 주니어캠프
```

---

## 🎨 UI 스크린샷 설명

### 관리자 페이지

#### FAQ 추가 폼
```
┌─────────────────────────────────────┐
│ 질문 *                              │
│ [캠프 인원이 몇 명인가요?]          │
│                                     │
│ 답변 *                              │
│ [약 120명입니다...]                 │
│                                     │
│ 카테고리 *                          │
│ [등록 ▼]                            │
│                                     │
│ 캠프 위치 *                         │
│ [🏝️ SMIS 제주캠프 ▼]              │
│  🏝️ SMIS 제주캠프                  │
│  🌴 SMIS 싱가포르&말레이시아 주니어 │
│  👨‍👩‍👧‍👦 SMIS 말레이시아 가족캠프     │
│  🌏 공통 (모든 캠프)                │
│                                     │
│ 💡 "공통"을 선택하면 모든 캠프에서  │
│    표시됩니다                       │
│                                     │
│              [취소]  [추가]         │
└─────────────────────────────────────┘
```

#### FAQ 목록 필터
```
┌─────────────────────────────────────┐
│ FAQ 목록 (10개)                     │
│                        [🗺️ ▼] [⚡ ▼]│
│                         캠프   카테고리│
└─────────────────────────────────────┘
```

#### FAQ 항목
```
┌─────────────────────────────────────┐
│ 🏝️ SMIS 제주캠프  [등록 ▼] 2026.04.14│
│                                     │
│ 캠프 인원이 몇 명인가요?             │
│                                     │
│ 28기 제주 여름캠프 기준 학생 약...  │
│                                     │
│ #인원 #학생 #반             [✏️] [🗑️]│
└─────────────────────────────────────┘
```

### 챗봇 위젯

#### 헤더
```
┌─────────────────────────────────────┐
│ 🤖 SMIS 캠프 FAQ    [🗺️ 제주캠프 ▼]│
│    무엇이든 물어보세요               │
└─────────────────────────────────────┘
```

#### 캠프 선택 드롭다운
```
┌─────────────────────────────────────┐
│ 🏝️ SMIS 제주캠프                   │
│ 🌴 SMIS 싱가포르&말레이시아 주니어  │
│ 👨‍👩‍👧‍👦 SMIS 말레이시아 가족캠프      │
└─────────────────────────────────────┘
```

---

## 🔄 데이터 흐름

### 관리자가 FAQ 추가
```
1. 관리자: FAQ 입력 + 위치 선택 (je/s/f/common)
   ↓
2. Firestore: location 필드 저장
   {
     question: "...",
     answer: "...",
     category: "등록",
     location: "je"  ← 새로 추가!
   }
   ↓
3. 챗봇: 해당 위치에서만 표시
```

### 유저가 챗봇 사용
```
1. 유저: 캠프 선택 (예: 제주캠프)
   ↓
2. 챗봇: location 필터링
   - location === 'je' OR location === 'common'
   ↓
3. AI: 필터링된 FAQ에서 검색
   ↓
4. 유저: 답변 확인
```

---

## 📈 필터링 로직

### 관리자 페이지
```typescript
// 카테고리 + 위치 이중 필터링
if (selectedCategory === 'all' && selectedLocation === 'all') {
  // 모든 FAQ 표시
  filteredFaqs = faqs;
} else if (selectedCategory === 'all') {
  // 위치만 필터링 (공통 포함)
  filteredFaqs = faqs.filter(faq => 
    faq.location === selectedLocation || faq.location === 'common'
  );
} else if (selectedLocation === 'all') {
  // 카테고리만 필터링
  filteredFaqs = faqs.filter(faq => 
    faq.category === selectedCategory
  );
} else {
  // 카테고리 + 위치 모두 필터링
  filteredFaqs = faqs.filter(faq => 
    faq.category === selectedCategory && 
    (faq.location === selectedLocation || faq.location === 'common')
  );
}
```

### 챗봇
```typescript
// 1. 위치 필터링 (선택한 캠프 + 공통)
faqs = faqs.filter(faq => 
  faq.location === selectedLocation || faq.location === 'common'
);

// 2. 카테고리 필터링 (선택사항)
if (selectedCategory !== 'all') {
  faqs = faqs.filter(faq => 
    faq.category === selectedCategory
  );
}
```

---

## 🎯 공통 FAQ의 활용

### "공통" FAQ란?
- 모든 캠프에 적용되는 FAQ
- 예: 휴대폰 관리, 용돈 관리, 통화 규칙 등

### 장점
- ✅ FAQ 중복 입력 불필요
- ✅ 한 번 수정하면 모든 캠프에 반영
- ✅ 유지보수 용이

### 예시
```
질문: 휴대폰은 어떻게 관리하나요?
위치: 공통 (common)

→ 제주캠프 선택 시: 표시됨 ✅
→ 싱가포르 캠프 선택 시: 표시됨 ✅
→ 말레이 가족캠프 선택 시: 표시됨 ✅
```

---

## 🔧 트러블슈팅

### Q1: 기존 FAQ가 안 보여요!
**A:** 마이그레이션 스크립트를 실행하세요.
```bash
npm run migrate:add-location
```

### Q2: "공통" FAQ가 특정 캠프에서만 보여요!
**A:** FAQ의 `location` 필드를 `'common'`으로 변경하세요.

### Q3: 새 캠프를 추가하고 싶어요!
**A:** `lib/types.ts`의 `LOCATIONS` 배열에 추가하세요.
```typescript
{ 
  id: 'new' as const, 
  name: 'SMIS 새로운캠프', 
  emoji: '🎪',
  description: '새로운 캠프 설명'
},
```

---

## 📊 통계 (예상)

### FAQ 분포
```
제주캠프: 40개
싱가포르 캠프: 30개
말레이 가족캠프: 20개
공통: 10개
───────────────
총: 100개
```

### 챗봇 검색 범위
- **제주 선택 시**: 40개 (제주) + 10개 (공통) = 50개
- **싱가포르 선택 시**: 30개 (싱가포르) + 10개 (공통) = 40개
- **말레이 가족 선택 시**: 20개 (말레이) + 10개 (공통) = 30개

---

## 🎉 최종 체크리스트

- ✅ 타입 정의 완료
- ✅ 관리자 페이지 - FAQ 입력 폼 수정
- ✅ 관리자 페이지 - 위치 필터 추가
- ✅ 관리자 페이지 - FAQ 목록 위치 배지 표시
- ✅ 챗봇 - 헤더 캠프 선택 추가
- ✅ 챗봇 - 위치 필터링 로직 구현
- ✅ 챗봇 - 추천 질문 위치 필터링
- ✅ 마이그레이션 스크립트 작성
- ✅ 타입 에러 없음
- ✅ 문서화 완료

---

**업데이트 날짜:** 2026-04-14  
**버전:** 1.5.0  
**핵심 기능:** 캠프 위치별 FAQ 관리 및 필터링

🎉 이제 3개 캠프의 FAQ를 효율적으로 관리할 수 있습니다!
