rules_version = '2';

// Firebase Storage Security Rules
service firebase.storage {
  match /b/{bucket}/o {
    // 모든 파일에 대한 읽기 권한 허용
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}